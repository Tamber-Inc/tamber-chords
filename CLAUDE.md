Use pnpm as the package manager. Use vitest for testing. Use esbuild for bundling.

- Use `pnpm install` to install dependencies
- Use `pnpm run <script>` to run scripts
- Use `pnpm run test` to run tests (vitest)
- Use `pnpm run build` to build (esbuild + tsc)
